import React from 'react';
import PropTypes from 'prop-types';
import '../../index.css';

const Button = () => {

  return (
    <button className='button'>
      Button
    </button>
  );

};

// Button.propTypes = {
//
// };

export default Button;
